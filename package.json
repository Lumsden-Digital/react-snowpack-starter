{
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1"
  },
  "scripts": {
    "start": "npm run watch:css && snowpack dev",
    "build": "npm run watch:css && snowpack build",
    "watch:css": "postcss src/assets/tailwind.css -o src/assets/main.css"
  },  
  "devDependencies": {
    "autoprefixer": "^10.3.3",
    "postcss-cli": "^8.3.1",
    "snowpack": "^3.0.1",
    "tailwindcss": "^2.2.7"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
